// import { Orders } from '../orders.entity';
// import { ordersStub, orderStub } from './product.stub';

// export const mockProductsRepository = {
//   find: jest.fn().mockResolvedValue(ordersStub()),
//   findOneOrFail: jest.fn().mockResolvedValue(orderStub()),
//   findOneBy: jest.fn().mockResolvedValue(orderStub()),
//   create: jest.fn().mockResolvedValue(orderStub()),
//   save: jest.fn((order: Orders) => order),
//   delete: jest.fn((id: number) => id),
// };
